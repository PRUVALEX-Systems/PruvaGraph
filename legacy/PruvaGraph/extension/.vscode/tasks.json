{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build:pruvagraph",
            "type": "shell",
            "command": "npm run build",
            "group": "build"
        }
    ]
}
